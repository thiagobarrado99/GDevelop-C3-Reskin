// @flow
// c3: the fork is called Assemble3. Every translated string that names
// GDevelop is rewritten with this (see C3Terminology.js); the few
// hardcoded spots (window title, start page, index.html) use it directly.
export const APP_NAME = 'Assemble3';
